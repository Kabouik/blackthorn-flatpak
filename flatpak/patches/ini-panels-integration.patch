diff --git a/node-src/integration.ts b/node-src/integration.ts
index 1f1f79c0..6f237dc5 100644
--- a/node-src/integration.ts
+++ b/node-src/integration.ts
@@ -1,0 +1 @@
+import { watch } from "node:fs";
@@ -36,6 +37,8 @@ import {
   applyPanelSnapshot,
   deletePanelSnapshot,
   listPanelSnapshots,
+  listIniSections,
+  listCharactersInIniDirectory,
   getNewCharacterPanelDefaultStatus,
   renamePanelSnapshot,
   resolvePanelSnapshotDirectory,
@@ -399,4 +401,48 @@ export function createLauncherServiceHandlers(
 ): LauncherServiceHandlers {
   const { context, options } = params;
   const activeDaocProcessId = createActiveDaocProcessIdGetter(params);
+  const DEFERRED_SECTIONS = new Set(["Chat"]);
+  const deferredWatchers = new Map<string, () => void>();
+  const scheduleDeferredApply = (job: {
+    characterName: string;
+    snapshotName: string;
+    sections: string[];
+    iniDirectory: string;
+    snapshotDirectory: string;
+  }): void => {
+    deferredWatchers.get(job.characterName)?.();
+    const iniPath = path.join(job.iniDirectory, `${job.characterName}-5.ini`);
+    let settled = false;
+    let debounce: ReturnType<typeof setTimeout> | null = null;
+    const watcher = watch(iniPath, { persistent: false }, (eventType) => {
+      if (settled || eventType !== "change") return;
+      if (debounce) clearTimeout(debounce);
+      debounce = setTimeout(() => {
+        settled = true;
+        watcher.close();
+        deferredWatchers.delete(job.characterName);
+        void applyPanelSnapshot({
+          iniDirectory: job.iniDirectory,
+          snapshotDirectory: job.snapshotDirectory,
+          characterName: job.characterName,
+          snapshotName: job.snapshotName,
+          sections: job.sections,
+        })
+          .then(() =>
+            options.logger?.info("[INI panels] deferred apply done", { characterName: job.characterName }),
+          )
+          .catch((err: unknown) =>
+            options.logger?.error("[INI panels] deferred apply failed", {
+              characterName: job.characterName,
+              err: String(err),
+            }),
+          );
+      }, 800);
+    });
+    deferredWatchers.set(job.characterName, () => {
+      settled = true;
+      if (debounce) clearTimeout(debounce);
+      watcher.close();
+    });
+  };
   return {
@@ -401,7 +446,37 @@ export function createLauncherServiceHandlers(
         snapshotDirectory: resolvePanelSnapshotDirectory(context.config.file),
         snapshotName,
       }),
-    snapshotCharacterPanels: async (characterName: string) => {
+    listCharacterIniSections: async (characterName: string) => {
+      const config = await context.config.load();
+      const iniDirectory = resolveSpecSwapDirectory({
+        platform: context.platform,
+        environment: context.environment,
+        winePrefix: config.uiOptions.linuxWinePrefix,
+      });
+      if (options.cefLifecycle) {
+        try {
+          await options.cefLifecycle.requestProfileIni("save", characterName);
+        } catch {
+          // game not running or flush failed — read from disk as-is
+        }
+      }
+      return listIniSections(
+        path.join(iniDirectory, `${characterName}-5.ini`),
+      );
+    },
+    listCharactersInIniDirectory: async () => {
+      const config = await context.config.load();
+      const iniDirectory = resolveSpecSwapDirectory({
+        platform: context.platform,
+        environment: context.environment,
+        winePrefix: config.uiOptions.linuxWinePrefix,
+      });
+      return listCharactersInIniDirectory(iniDirectory);
+    },
+    snapshotCharacterPanels: async (
+      characterName: string,
+      sections?: string[],
+    ) => {
       const pid = await activeDaocProcessId();
       if (!options.cefLifecycle)
         throw new Error("CEF game bridge is not configured.");
@@ -420,14 +488,29 @@ export function createLauncherServiceHandlers(
         iniDirectory,
       });
       await options.cefLifecycle.requestProfileIni("save", characterName);
+      let width: number | undefined;
+      let height: number | undefined;
+      try {
+        const viewport = await options.cefLifecycle.getGameViewport();
+        width = viewport.width;
+        height = viewport.height;
+      } catch {
+        // viewport not critical — omit silently
+      }
       options.logger?.info("[INI panels] writing panel snapshot", {
         characterName,
         snapshotDirectory,
+        sections,
+        width,
+        height,
       });
       const entry = await snapshotCharacterPanels({
         iniDirectory,
         snapshotDirectory,
         characterName,
+        sections,
+        width,
+        height,
       });
       options.logger?.info("[INI panels] panel snapshot written", entry);
       return entry;
@@ -473,7 +556,11 @@ export function createLauncherServiceHandlers(
         ...viewport,
       });
     },
-    applyPanelSnapshot: async (characterName: string, snapshotName: string) => {
+    applyPanelSnapshot: async (
+      characterName: string,
+      snapshotName: string,
+      sections?: string[],
+    ) => {
       const pid = await activeDaocProcessId();
       if (!options.cefLifecycle)
         throw new Error("CEF game bridge is not configured.");
@@ -497,15 +584,25 @@ export function createLauncherServiceHandlers(
         characterName,
       });
       await options.cefLifecycle.requestProfileIni("save", characterName);
-      options.logger?.info("[INI panels] replacing target [Panels] section", {
+      options.logger?.info("[INI panels] replacing target INI sections", {
         characterName,
         snapshotName,
+        sections,
       });
-      await applyPanelSnapshot({
-        iniDirectory,
-        snapshotDirectory,
-        characterName,
-        snapshotName,
-      });
+      const immediate = sections?.filter((s) => !DEFERRED_SECTIONS.has(s));
+      const deferred = sections?.filter((s) => DEFERRED_SECTIONS.has(s)) ?? [];
+      if (!immediate || immediate.length > 0) {
+        await options.cefLifecycle.requestProfileIni("reload", characterName);
+        await applyPanelSnapshot({
+          iniDirectory,
+          snapshotDirectory,
+          characterName,
+          snapshotName,
+          sections: immediate,
+        });
+      }
+      if (deferred.length > 0) {
+        scheduleDeferredApply({ characterName, snapshotName, sections: deferred, iniDirectory, snapshotDirectory });
+      }
       options.logger?.info("[INI panels] requesting live profile reload", {
         characterName,
@@ -516,6 +615,69 @@ export function createLauncherServiceHandlers(
         snapshotName,
       });
+      return { deferred };
     },
+    applyPanelSnapshotToAllCharacters: async (
+      snapshotName: string,
+      sections?: string[],
+    ) => {
+      await activeDaocProcessId();
+      if (!options.cefLifecycle)
+        throw new Error("CEF game bridge is not configured.");
+      const config = await context.config.load();
+      const iniDirectory = resolveSpecSwapDirectory({
+        platform: context.platform,
+        environment: context.environment,
+        winePrefix: config.uiOptions.linuxWinePrefix,
+      });
+      const snapshotDirectory = resolvePanelSnapshotDirectory(
+        context.config.file,
+      );
+      const characters = await listCharactersInIniDirectory(iniDirectory);
+      options.logger?.info("[INI panels] applying snapshot to all characters", {
+        snapshotName,
+        sections,
+        characters,
+      });
+      const activeWindows = context.state.activeWindows ?? [];
+      const deferredFor: string[] = [];
+      for (const characterName of characters) {
+        const isActive = activeWindows.includes(characterName);
+        const immediate = isActive
+          ? sections?.filter((s) => !DEFERRED_SECTIONS.has(s))
+          : sections;
+        const deferredSections = isActive
+          ? (sections?.filter((s) => DEFERRED_SECTIONS.has(s)) ?? [])
+          : [];
+        await options.cefLifecycle.requestProfileIni("save", characterName);
+        if (isActive && (!immediate || immediate.length > 0)) {
+          await options.cefLifecycle.requestProfileIni("reload", characterName);
+        }
+        if (!immediate || immediate.length > 0) {
+          await applyPanelSnapshot({
+            iniDirectory,
+            snapshotDirectory,
+            characterName,
+            snapshotName,
+            sections: immediate,
+          });
+        }
+        if (deferredSections.length > 0) {
+          scheduleDeferredApply({
+            characterName,
+            snapshotName,
+            sections: deferredSections,
+            iniDirectory,
+            snapshotDirectory,
+          });
+          deferredFor.push(characterName);
+        }
+      }
+      options.logger?.info(
+        "[INI panels] apply-to-all completed",
+        { snapshotName, characters },
+      );
+      return { deferredFor };
+    },
     watchChatLogForItem: (file: string, signal?: AbortSignal) =>
       context.chatLog.watch(file, { signal }),
     cancelChatLogWatch: () => context.chatLog.cancel(),
